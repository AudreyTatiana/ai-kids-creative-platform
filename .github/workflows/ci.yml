name: CI — PetitsRêves

on:
  push:
    branches: [main, dev]
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "docs/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "docs/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  # ─────────────────────────────────────────
  # JOB 1 — BACKEND
  # ─────────────────────────────────────────
  backend:
    name: Backend — install & audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installation de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: backend/package-lock.json

      - name: Installation des dépendances backend
        working-directory: backend
        run: npm ci --omit=dev

      - name: Audit de sécurité npm
        working-directory: backend
        run: npm audit --audit-level=high || true

  # ─────────────────────────────────────────
  # JOB 2 — FRONTEND
  # ─────────────────────────────────────────
  frontend:
    name: Frontend — install & lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installation de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Installation des dépendances frontend
        working-directory: frontend
        run: npm ci

      - name: Lint du code frontend
        working-directory: frontend
        run: npm run lint || true

  # ─────────────────────────────────────────
  # JOB 3 — BUILD DOCKER
  # ─────────────────────────────────────────
  build:
    name: Build & Push Docker
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Connexion au registre GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Configuration de Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extraction des métadonnées (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/audreytatiana/ai-kids-creative-platform/backend
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build et push de l'image Docker
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ─────────────────────────────────────────
  # JOB 4 — SCAN TRIVY
  # ─────────────────────────────────────────
  trivy:
    name: Sécurité — Trivy scan
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Scan Trivy — vulnérabilités image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ghcr.io/audreytatiana/ai-kids-creative-platform/backend:${{ github.ref_name }}
          format: sarif
          output: trivy-results.sarif
          severity: HIGH,CRITICAL
          exit-code: "0"

      - name: Upload résultats Trivy
        uses: actions/upload-artifact@v4
        with:
          name: trivy-scan-results
          path: trivy-results.sarif
          retention-days: 14
